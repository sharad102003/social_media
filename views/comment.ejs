<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Comments</title>
    <link href="https://maxcdn.bootstrapcdn.com/font-awesome/4.3.0/css/font-awesome.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>

<body class="bg-black text-white">
    <div class="container mx-auto py-8">
        <div class="bg-gray-800 p-8 rounded-lg shadow-lg">
            <h1 class="text-2xl font-bold mb-4">Comments (<%= posts.comments.length %>)</h1>
            <div class="space-y-6">
                <% posts.comments.forEach(function(elem) { %>
                <div class="flex items-start space-x-4">
                    <div class="flex-shrink-0">
                        <a href="blog-detail-2.html">
                            <!-- Change made here: Removed '/images/uploads/' prefix assuming elem.user.profileImage is a full URL -->
                            <img src="<%= elem.user.profileImage %>" alt="" class="w-12 h-12 rounded-full">
                        </a>
                    </div>
                    <div class="flex-1 bg-gray-700 p-4 rounded-lg">
                        <div class="flex justify-between items-center mb-2">
                            <p class="font-bold text-sm"><%= elem.user.username %></p>
                            <span class="text-xs text-gray-400">
                                <i class="fa fa-clock-o"></i>
                                <%= elem.date %>
                            </span>
                        </div>
                        <p class="text-gray-300 text-sm">
                            <%= elem.text %>
                        </p>
                    </div>
                </div>
                <% }) %>
            </div>
            <form id="uploadform" action="/commentupload/<%= posts._id %>" method="post" class="mt-8">
                <div class="mb-4">
                    <textarea name="comment" class="w-full p-4 text-sm bg-gray-700 rounded-lg text-white" required placeholder="Your comment"></textarea>
                </div>
                <div>
                    <input class="w-full px-4 py-2 bg-blue-500 rounded-lg text-white font-semibold cursor-pointer" type="submit" value="Post">
                </div>
            </form>
        </div>
    </div>
</body>

<script>
    // Optionally, you can add some JavaScript here if needed
</script>

</html>




















